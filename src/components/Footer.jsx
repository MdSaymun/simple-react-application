const Footer = () => {
  return <footer>Programming is the closest thing we have to magic, where mere thoughts and logic can transform into creations that empower and inspire</footer>;
};

export default Footer;
